{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit",
        "hooks": [
          {
            "type": "command",
            "command": "file=$(jq -r '.tool_input.file_path'); echo \"$file\" | grep -q '\\.dart$' && command -v flutter > /dev/null 2>&1 && cd apps/mobile && flutter analyze --no-fatal-warnings 2>&1 | tail -10 || true",
            "statusMessage": "Analizando Dart..."
          }
        ]
      }
    ]
  }
}
