FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm install --legacy-peer-deps

COPY . .

EXPOSE 3000

# Instalar deps al arrancar (el volumen monta el host y puede dejar node_modules vacío)
CMD ["sh", "-c", "npm install && npm run start:dev"]
